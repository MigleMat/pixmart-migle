<nav class="{{ navClass }} nav">
    <ul class="nav__list">
        {% for navItem in navItems %}
            <li class="nav__item">
                <a class="nav__link" href="{{navItem.link}}">{{ navItem.label }}</a>
            </li>
        {% endfor %}
    </ul>
</nav>